<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.tedu.csmall.sever.Mapper.AttributeTamplateMapper">
    <!--int insert(AttributeTample)-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into pms_attribute_template(name, pinyin, keywords, sort, gmt_create, gmt_modified)
        values (#{name},#{pinyin},#{keywords},#{sort},#{gmtCreate},#{gmtModified})
    </insert>
</mapper>